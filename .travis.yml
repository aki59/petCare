sudo: false
language: java
dist: trusty
jdk: oraclejdk8

install: true

script: mvn clean package -DskipTests=true


after_success:
  - bash <(curl -s https://codecov.io/bash)
  
cache:
  directories:
    - '$HOME/.m2/repository'